package org.shubham.airline.ui.theme

import org.shubham.airline.model.AppColors

// Light theme
val LightThemeColors = AppColors(
    background = LightBackground,
    textPrimary = LightTextPrimary,
    card = LightCard,
    primary = BluePrimary,
    secondary = skyBlue,
    accent = OrangeLight
)

// Dark theme
val DarkThemeColors = AppColors(
    background = DarkBackground,
    textPrimary = DarkTextPrimary,
    card = DarkCard,
    primary = BlueDark,
    secondary = BlueLight,
    accent = OrangeDark
)